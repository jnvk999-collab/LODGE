# Test tooling. lodge/test/smoke-test.js needs Playwright, which the
# README tells you to install with `npm i -D playwright`. That creates
# these; none of them belong in the repository.
node_modules/
package.json
package-lock.json

# OS and editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
